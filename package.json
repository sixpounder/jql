{
  "name": "sequel",
  "version": "0.1.0",
  "description": "A sql-like querying interface for javascript objects and the DOM",
  "engines": {
    "node": ">=18.18.0"
  },
  "exports": {
    ".": {
      "types": "./lib/sequel.d.ts",
      "main": "./lib/sequel.js",
      "module": "./sequel.mjs",
      "default": "./sequel.mjs"
    }
  },
  "scripts": {
    "build": "rimraf ./dist && tsc && npm run build:mjs && npm run build:cjs && npm run copy-assets",
    "build:mjs": "esbuild ./src/index.ts --bundle --format=esm --outfile=dist/sequel.mjs",
    "build:cjs": "esbuild ./src/index.ts --bundle --format=cjs --outfile=dist/sequel.cjs",
    "copy-assets": "node build/assets.cjs dist && cp README.md dist/README.md",
    "lint": "eslint --fix .",
    "link": "npm run build && cd dist && npm link",
    "publish-package": "npm run build && cd dist && npm publish",
    "test": "jest --coverage"
  },
  "author": "Andrea Coronese <sixpounder@protonmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/jsdom": "^21.1.5",
    "@types/lodash-es": "^4.17.12",
    "@typescript-eslint/eslint-plugin": "^6.10.0",
    "@typescript-eslint/parser": "^6.10.0",
    "esbuild": "^0.19.5",
    "eslint": "^8.53.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jsdom": "^22.1.0",
    "lodash": "^4.17.21",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "lodash-es": "^4.17.21"
  }
}
